server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # Permite subir fotos de hasta 50MB y videos de hasta 250MB
  client_max_body_size 250m;

  # SPA routing — todas las rutas van a index.html
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Resolver DNS interno de Docker — re-resuelve el hostname de la API dinámicamente
  resolver 127.0.0.11 valid=10s ipv6=off;

  # Proxy a la API (incluye uploads multipart)
  location /api {
    set $upstream http://gutleber-api:3001;
    proxy_pass $upstream;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_read_timeout 120s;
    proxy_send_timeout 120s;
    proxy_request_buffering off;
  }

  # Archivos subidos (fotos, videos de propiedades)
  location /uploads {
    set $upstream http://gutleber-api:3001;
    proxy_pass $upstream;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    add_header Cache-Control "public, max-age=604800";
  }
}
