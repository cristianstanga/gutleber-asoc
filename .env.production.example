# Copiá este archivo como .env.production y completá los valores
# cp .env.production.example .env.production

# Dominio (sin https://)
DOMAIN=crm.gutleberyasociados.com

# Base de datos — elegí una contraseña segura
DB_PASSWORD=CambiarPorPasswordSeguro123!

# JWT — string largo y aleatorio (podés generar con: openssl rand -hex 32)
JWT_SECRET=cambiar_por_string_largo_aleatorio_aqui
